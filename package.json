{
  "name": "pareto-buildenvironment",
  "version": "0.312.0",
  "dependencies": {
    "npm-updatedependencies2latest": "^0.0.10",
    "typescript": "^5.0.2"
  },
  "scripts": {
    "publish x": "npm version minor && git push && npm publish"
  }
}
