{
  "name": "pareto-buildenvironment",
  "version": "0.255.0",
  "dependencies": {
    "npm-updatedependencies2latest": "^0.0.5",
    "typescript": "^4.9.5"
  },
  "scripts": {
    "publish x": "npm version minor && git push && npm publish"
  }
}
